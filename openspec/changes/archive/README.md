# 归档变更

已完成并验证通过的变更，归档到本目录，命名格式：

```
archive/<YYYY-MM-DD>-<change-id>/
```

归档条件（全部满足）：

1. 变更的 verify gate 已通过（测试 0 failures + 实现与 delta 一致）。
2. delta 已按需求粒度合并进 `openspec/specs/` 对应文件。
3. proposal / design / tasks 已回填实际执行结果。

（暂无归档变更。）
